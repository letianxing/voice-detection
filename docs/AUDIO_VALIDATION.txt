Voice能力与验证结果（2026-09-15）

完成内容
- 逐帧声学信息：RMS dBFS、原始通道RMS、可选声压校准、阵列方向与有效性。
- 不依赖ASR的响声惊跳、后方声音定向，带标定、持续时间、不应期、回声/已检测音乐抑制。
- 本地AST音乐存在性模型 + 十类曲风模型，模型文件固定revision、safetensors、来源与SHA256记录；约691MiB权重。
- 起音/节奏分析得到BPM，并用估计节拍相位验证实时起音脉冲；低可信度返回无效。
- ROS2话题发布，含ASR、方向、惊跳、转向、音乐状态、曲风、BPM、节拍、精确follow/stop/hello意图，以及完整诊断状态。
- 默认ASR与意图先通过Attention资格门；旁人原始转写保留供记忆使用。
- 正式robo_brain_service的ROSBridge和CDR接收均保留浮点声纹；保留原始说话人标签、稳定数字映射、角度有效性；过期角度失效且不借用上一说话人的方向。
- biomimetic-brain-test可显示声学/音乐信息，记录反射和节拍意图及记忆事件，不驱动电机。
- Voice内遮挡包导入的空robot_attention_perception目录已清理；Attention内正常Python源码包保留。

验证结果
1. Voice 49项、Attention 38项、Brain测试版12项Python测试通过，共99项。
2. 正式大脑aggregator、identity、gaze、service/generated相关Go包测试通过；新增ROSBridge接收测试单独通过。
3. 正式大脑cmd/robo_brain成功编译。
4. ROS2 Jazzy镜像重新构建成功，包含从现有源码引用的audio_msgs消息包。
5. 隔离ROS2容器（DOMAIN_ID=173，端口19090）真实收到ASR、方向、两种反射、音乐状态、BPM、曲风、节拍及AudioIntent；验证浮点声纹未丢失，前方0度正确映射为正式大脑的90度。
6. 中文语音录音：AST音乐分数约0.005，未当作音乐。
7. 管弦乐录音回放：检测到music，曲风classical分数约0.97；异步实时路径得到有效BPM估计，静音后状态清除，单麦方向保持未知；分类推理一次约458ms。
8. 120 BPM合成节奏测试通过；无声不发伪节拍，半拍起音不重复当成整拍。
9. 源码精确重复检查见同目录“代码重复性检查.txt”。

未宣称通过的部分
- 原有整包ROSBridge测试引用已不存在的newMappingStatusEvent、EventIdleSlamMappingStatus、topicMappingStatus，导致该整包测试无法编译。已在修改前的Git HEAD中确认这些遗留引用；没有为使测试变绿而屏蔽旧测试或恢复无关SLAM功能。源码主程序可编译，本次接收测试已单独运行。
- 曲风模型可能误判；低置信度保留unknown。十类标签之外的音乐不能保证正确归类。
- BPM可能有半速/倍速歧义。真实房间中的回声、多说话人、麦克风几何和反射阈值仍需现场验收。
- 本次只在隔离ROS容器验证发布，没有向实体机器人下发动作。

操作入口
/Applications/声音/Mac全系统启动与三项验收.txt（末尾第十二节为此次新增步骤）
~/Golands/voice-detection/docs/AUDIO_CAPABILITIES.md（完整topic、模型、坐标与配置说明）

正式大脑已经修改源码，需要重新部署其新构建版本；旧运行实例不会自动获得修复。

后续启动排查（2026-09-15）
- 新增真正的一键入口start_all.sh，包含Qwen和七个服务；分开启动共用同一份配置，统一检查模型/依赖并管理进程组。
- 修复旧录音回调等待ASR finish及声纹提取的问题，移至后台finalizer；超时partial不再误报为final，停止后的旧识别任务不能污染新采集。
- 补齐MediaPipe依赖；1.0.1在本机手势加载时发生原生崩溃，切换并固定0.10.32后，五个视觉模型与实际摄像头通过检查。
- 将声学处理依赖初始化移到录音开始前；一键启动实测音频scene丢帧数0、finalizer丢帧数0。
- 实际验证了分别启动与一键启动、重复启动拦截、统一停止和端口释放。测试期间暂时静音TTS；新启动仍默认开启TTS。
- Sherpa、Vosk离线识别均返回最终文字；Sherpa子进程无残留。SpeakerBeam CPU模型输出形状通过检查。
- 启动说明精简为两块，注册与详细验收另存“现场注册与验收.txt”。

2026-09-15 实时回话与反射补充
Voice 55、Attention 46、Brain 27、HRI 5 个离线测试；另外8092 DOM脚本检查通过。
覆盖：分句流式播放与取消、回声归属、定向打断/旁人不打断、惊跳短叹去重、空间坐标有效性、主人身份波动不重复问候、设备独立切换、历史分页与时间线。
所有全系统服务按用户要求关闭；本轮修改后没有重开麦克风/摄像头，外放打断仍需现场验收。
实测样本与测试步骤见 /Applications/声音/现场注册与验收.txt。不能由离线测试推断现场准确率或保证“最快”。
